pandas>=2.0
numpy>=1.24
PyYAML>=6.0
streamlit>=1.30
pytest>=7.4

# Optional — only needed if you set ANTHROPIC_API_KEY and want live LLM
# narrative synthesis instead of the deterministic template fallback.
anthropic>=0.34
